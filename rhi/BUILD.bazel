load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "context",
    hdrs = [
        "context.h",
    ],
    srcs = [
        "context.cpp",
    ],
    deps = [
        "@volk//:volk",
        "@glfw//:glfw",
        "//core:core",
    ],
    visibility = ["//visibility:public"],
)
