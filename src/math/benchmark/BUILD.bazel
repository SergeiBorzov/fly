load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "functions",
    srcs = [
        "benchmark_functions.cpp",
    ],
    deps = [
        "//src/core:clock",
        "//src/math:math",
    ],
)

