load("@rules_cc//cc:defs.bzl", "cc_library", "cc_binary")

cc_binary(
    name = "functions",
    srcs = [
        "benchmark_functions.cpp",
    ],
    deps = [
        "//core:clock",
        "//math:math",
    ],
)

